CREATE TABLE t (a INT PRIMARY KEY, b INT NOT NULL);
INSERT INTO t VALUES (1, 1);
ALTER TABLE t ALTER PRIMARY KEY USING COLUMNS (b);
ALTER TABLE t ALTER PRIMARY KEY USING COLUMNS (a);
SELECT * FROM t@t_b_key;
