SET optimizer_switch = 'hypergraph_optimizer=on';
   CREATE TABLE t (x INTEGER);
INSERT INTO t VALUES (1), (2), (3);
ANALYZE TABLE t;
EXPLAIN FORMAT=TREE SELECT DISTINCT MIN(t2.x), t1.x FROM t t1 JOIN t t2 USING (x) GROUP BY t2.x;
DROP TABLE t;
SET optimizer_switch = 'hypergraph_optimizer=on';
   CREATE TABLE t (x INTEGER);
INSERT INTO t VALUES (1), (2), (3);
ANALYZE TABLE t;
EXPLAIN FORMAT=TREE SELECT DISTINCT MIN(t2.x), t1.x FROM t t1 JOIN t t2 USING (x) GROUP BY t2.x;
DROP TABLE t;
SET optimizer_switch = 'hypergraph_optimizer=on';
   CREATE TABLE t (x INTEGER);
INSERT INTO t VALUES (1), (2), (3);
ANALYZE TABLE t;
EXPLAIN FORMAT=TREE SELECT DISTINCT MIN(t2.x), t1.x FROM t t1 JOIN t t2 USING (x) GROUP BY t2.x;
DROP TABLE t;
SET optimizer_switch = 'hypergraph_optimizer=on';
   CREATE TABLE t (x INTEGER);
INSERT INTO t VALUES (1), (2), (3);
ANALYZE TABLE t;
EXPLAIN FORMAT=TREE SELECT DISTINCT MIN(t2.x), t1.x FROM t t1 JOIN t t2 USING (x) GROUP BY t2.x;
DROP TABLE t;
SET optimizer_switch = 'hypergraph_optimizer=on';
   CREATE TABLE t (x INTEGER);
INSERT INTO t VALUES (1), (2), (3);
ANALYZE TABLE t;
EXPLAIN FORMAT=TREE SELECT DISTINCT MIN(t2.x), t1.x FROM t t1 JOIN t t2 USING (x) GROUP BY t2.x;
DROP TABLE t;
